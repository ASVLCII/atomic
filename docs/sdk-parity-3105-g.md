# SDK parity #3105: slice G invariant map

Baseline: `0fcbb98f6ac5fb5cc990c28d75ace9e3b8ad109c`, branch `feat/3105-sdk-g`. This map supplements F's lifecycle map; F ownership, authority retirement and admitted-work accounting are preserved. H packed consumers and publication are outside this slice.

## Source-grounded boundaries before edits

| Invariant | Materially distinct paths and ownership transitions | Dependencies / unaffected paths |
| --- | --- | --- |
| Node subprocesses settle only after owned process/output cleanup | `web-access/subprocess.ts:runBunSubprocess` is shared by video and YouTube duration/frame/stream extraction. It unconditionally calls Bun.spawn/sleep. Caller cancellation, timeout and output overflow terminate the child; error paths currently return before joining exit. | Preserve binary bytes, separate stderr cap, cwd/env and explicit ENOENT/nonzero errors. `github-extract.ts` uses node execFile already and does not call this adapter. No CLI subprocess is needed. |
| Service initialization remains lazy | MCP `index.ts:session_start` registered cached direct tools then unconditionally initialized and scheduled warmup; cleanup imports OAuth even for never-used instances. Intercom/web light wrappers defer heavy import using generation leases. | Keep direct tools discoverable from config/cache; configured eager servers may connect at SDK creation. Preserve F candidate cleanup and stale lease fencing. |
| MCP closes only its own connections/auth/UI | Manager and lifecycle are instance-local in `init.ts`; OAuth maps/barrier in `mcp-auth-flow.ts`, callback server/port, and active Glimpse window are module-global. Agent paths consult environment, not factory agentDir. | Wrapper, direct-tool, command and reconnect paths share manager/auth helpers. Cache is disk-backed keyed by server config hash, but credential/config location must follow owner. Retain auth callback CSRF checks and primary/cleanup errors. |
| Web results/config/background work belong to one session | `storage.ts` has a global result map; `web-search-features.ts` has global pendingFetches/runtimeState/activityState. Provider modules cache credential config at module scope. `github-extract.ts` globally caches clones and removes clone paths on session changes. | Light wrapper leases do not isolate these globals. Content tools, search, curator, session restore and shutdown share them. Immutable parsers and SQLite module imports can remain shared. |
| Intercom identity and authority are owner-specific | Heavy factory owns client, reply trackers and authorization registry. It writes/restores ambient INTERCOM_SESSION_ID; runtime-group uses session-keyed env entries. Broker is a separate process with Node launch support, socket registration is the client's lease. | Same-group and supervisor authorization remain broker checks, not tool-presence checks. Disconnect must unregister one identity only; never shut down the shared broker to close a session. Broker signal/exit handlers run in its separate executable, not the SDK host. |
| Diagnostics route to the owner without secrets or unsolicited output | HostDiagnostic exists on runner bindings, but service helpers still use console. MCP config, auth, logger and cleanup; Intercom light/heavy cleanup; web config/provider/extractor paths all share this requirement. | Operational diagnostics must not include remote error bodies, credentials, prompts or supervisor capabilities. Third-party console output is not intercepted. Existing error/tool results remain available. |
| Process state is not session authority | SDK sets one default stream adapter at module load; explicit Agent streamFn uses per-session runtime. F scopes workflow registration via lifecycleScope. Theme rendering is optional; service modules must not mutate cwd, stdin, signal handlers or console. | Preserve F workflow leases, input identity/cancellation, borrowed runtime semantics and resource loader discovery. Audit theme and environment call sites before claiming unaffected behavior. |

## Planned vertical evidence

Start with a Node-only shared subprocess regression, then service public SDK probes one boundary at a time. Reuse existing MCP URL interpolation/cleanup, Intercom group/auth/lease and web subprocess suites. Built non-TTY fixtures must exit naturally; no process.exit mask. Test independent release while operations are suspended, distinguish fresh admission from completion and retain primary plus cleanup failures.

## Executed coverage

| invariant | affected paths | exact executed assertion/log and candidate | residual gap |
| --- | --- | --- | --- |
| Node execution without Bun | Shared `runBunSubprocess`, video and YouTube callers | Inherited RED: `Node subprocess adapter executes without Bun and preserves binary output` failed `Bun is not defined`, `/tmp/3105-g-subprocess-red.log`. This continuation reran all 7 subprocess tests successfully in `/tmp/3105-g-services.log`. | Explicit separate stderr cap and independently held cancellation/overlap still need expanded assertions; no built web tool fixture. |
| Headless MCP lazy discovery | Default public SDK factory, uncached configured direct tools, first owned gateway use, local HTTP transport | RED `/tmp/3105-g-lazy-red.log`: startup made 7 HTTP requests where 0 were required, with no unhandled fixture errors. GREEN `/tmp/3105-g-lazy-green.log`, then complete source parity `/tmp/3105-g-sdk.log`: 160/160, exit 0. | Mixed eager/lazy warmup is covered by the subsequent row. Configured cached direct execution has no new G-specific probe; stdio shares the repaired transport-independent warmup boundary. |
| Mixed headless MCP startup preserves lazy connections | Public `createAgentSession` / `session_start`, `initializeSession` → `scheduleMcpStartupWarmup`; eager and keep-alive siblings share the same helper | `SDK mixed %s MCP startup preserves lazy discovery` covers both lifecycles: RED `/tmp/3105-g-mixed-red.log`, exit 1, both observed 7 lazy HTTP requests instead of 0. GREEN `/tmp/3105-g-mixed-green.log`, exit 0, 2/2: sibling connected, lazy requests = 0 after 2 seconds, `lazy_echo` absent, then first owned lazy gateway connect succeeds. Full source parity `/tmp/3105-g-mixed-sdk.log`: 165/165 including unchanged all-lazy control; built host parity `/tmp/3105-g-mixed-host.log`: 220/220; MCP units `/tmp/3105-g-mixed-mcp.log`: 26 files, 177/177; build/check `/tmp/3105-g-mixed-{build,check}.log`; all exit 0. | No new built mixed fixture or stdio matrix: HTTP and stdio both enter `manager.connect` from this warmup before transport selection in `server-manager.ts`. Existing built `sdk-host-lazy-mcp.mjs` remains the natural-exit oracle. Terminal warmup is preserved by passing `session.ctx.hasUI`, with the cancellation unit exercising uncached lazy connection under `hasUI: true`. No new terminal UI end-to-end probe; broader G residual gaps remain. |
| HTTP connection lifetime | Built public Node SDK, two distinct project directories, close second session then call first session's live HTTP MCP connection | `built Node lazy MCP HTTP ownership`, complete host parity `/tmp/3105-g-host.log`: 215/215, exit 0. Standalone `/tmp/3105-g-http-stdout.log`: `{"verified":true,"requests":8}`; stderr empty; natural exit 0. | This proves one connection control only, not OAuth/UI, diagnostic routing, group isolation, suspended cancellation or overlapping cleanup. |
| Preserve existing service/lifecycle behavior | All matching MCP, Intercom and web-access unit suites; explicit eager acquisition in existing retired-initializer fixtures | `npx vitest --run --project unit test/unit/mcp-*.test.ts test/unit/intercom-*.test.ts test/unit/web-access-*.test.ts`: 95 files, 1004/1004, exit 0; `/tmp/3105-g-services.log`. | Existing unit coverage is not new end-to-end service isolation evidence. |
| Build/type safety | Current partial implementation | `npm run build`, `/tmp/3105-g-build.log`, exit 0; `npm run check`, `/tmp/3105-g-check.log`, exit 0 (includes both typecheck passes and shrinkwrap check). | Slice G remains incomplete despite passing gates. |
| Service ownership/config/diagnostics | Remaining paths above | Source audit only for OAuth globals, Intercom identity and web state. MCP startup/logger diagnostics now have the executed evidence below. | Remaining G isolation and all-service redaction acceptance unverified; no complete MCP/Intercom/web isolation claim. |
| MCP startup/logger diagnostics | Eager missing stdio command; concurrent lazy gateway calls in two built Node SDK sessions; first-owner reload after second-owner close; default no-sink SDK | Source RED `/tmp/3105-g-diagnostic-red.log`: 2 unsolicited console errors. Source GREEN `/tmp/3105-g-diagnostic-green.log`: owner IDs and secret omission pass. Built RED `/tmp/3105-g-logger-red.log`: console DEBUG output and missing sink entry. Built GREEN `/tmp/3105-g-logger-green.log`: `{"verified":true}`, empty stderr, natural exit 0. Expanded fixture also passes with and without sinks, concurrent calls and reload. | Other direct console callsites in MCP config/auth/cleanup, Intercom and web remain. Callback-server logger ownership outside an admitted async context remains unverified. No OAuth/UI isolation claim. |
| Continuation regression gates | Full source SDK parity, built Node host parity, all matching service suites, build and check | `/tmp/3105-g-sdk-next.log`: 161/161; `/tmp/3105-g-host-next.log`: 217/217; `/tmp/3105-g-services-next.log`: 95 files, 1004/1004. `npm run build` and `npm run check`: exit 0, `/tmp/3105-g-{build,check}-next.log`. All test commands exited 0. | G remains partial: no executed ownership assertions for OAuth/UI, Intercom groups/leases, web globals, or suspended service cleanup. |
| Shared-loader web result ownership | One borrowed builtin loader, two SDK owners, local HTTP extraction, sibling initialization and overlapping close | Source RED `/tmp/3105-g-web-red.log`: first owner's `get_search_content` returned `No stored results` after sibling use/close. GREEN `/tmp/3105-g-web-green.log`; complete source parity `/tmp/3105-g-sdk-remaining.log`: 162/162. Per-heavy-instance result storage now flows through fetch, search return, restore, cleanup and result-list commands. | Independent default loaders already passed before the repair; sharing borrowed discovery is the materially distinct regression. Provider caches and clone maps remain global. |
| Web suspended work ownership | Instance-local pendingFetches/runtimeState/activityState; first owner's local HTTP request held while sibling reloads and closes twice; background notification admission held during sibling shutdown | Built `sdk-host-web-owners.mjs`: first result remains private and readable, suspended fetch completes, both results remain readable, `{"verified":true}`, empty stderr, natural exit. `web-search-features-error-path`: sibling shutdown does not abort first pending notification; its own shutdown does. | Background notification test uses existing controlled dependency fixture; full provider/curator UI, transfer failures and reentrancy are not established by it. |
| Intercom groups and independent leases | Built public Node SDK, two owners, lazy broker start, distinct groups, join/leave, same-group send, cross-group refusal, overlapping close | `sdk-host-intercom-owners.mjs`: no broker PID before first tool use; distinct identities; cross-group delivery false with refusal reason; same-group delivery true; closing first removes only its identity while sibling remains usable; unchanged broker PID survives both session closures. Natural exit with `{"verified":true}`, empty stderr. Fixture terminates its isolated broker only after all session-close assertions. | No new source fix needed for these exercised paths. Supervisor authority, suspended cancellation and transfer failure still rely on existing suites, not this built fixture. Ambient Intercom identity restoration remains unaudited for shared-loader ownership. |
| Continuation gates | Complete source/host parity; affected MCP, Intercom, web-access and web-search unit suites | `/tmp/3105-g-sdk-remaining.log`: 162/162; `/tmp/3105-g-host-remaining.log`: 219/219; `/tmp/3105-g-services-remaining.log`: 98 files, 1017/1017. Build and check exited 0 (`/tmp/3105-g-{build,check}-remaining.log`). Initial full-suite commands hit the external 120-second shell limit, then completed with 600-second shell budgets; test budgets were unchanged. | G remains partial. OAuth/UI owner scoping, web provider/clone caches, unavailable-dependency acceptance, remaining direct console redaction, and complete service transfer/reentrancy coverage remain outstanding. |

## Frozen web configuration boundary

Keep existing `getUserConfigPaths("web-search.json")` discovery and existing environment keys. Do not add an agentDir/project-local web configuration schema or session option, snapshot global credentials, or mutate the environment to emulate isolation. G isolates owner-scoped in-memory results, provider configuration caches, pending fetches, clone maps, and diagnostics. Existing model credentials remain separate from web API keys. This decision supersedes the earlier open web configuration question.

## Continuation audit

The prior diagnostic continuation found that `ExtensionAPI.lifecycleScope` supplies an internal owner identity, while the diagnostic sink is private to `ExtensionRunner` and used by `emitError`. Neither the extension API nor extension context exposes public service diagnostic emission. MCP OAuth pending authentication/transport maps, cleanup barrier, callback server, callback port, and callback waiters remain module-global. Web result storage and feature pending-fetch/runtime/activity state were also module-global before the latest repair below. These source findings alone do not establish isolation.

This continuation adds an internal diagnostic bridge from the existing runner listener, async owner context around extension work and wrapped tools, and redacted MCP startup/logger routing. No new public API or console interception was added. Root `ISSUES.md` remains unchanged because G isolation is not complete. No H work or publication was performed.

The latest continuation repairs the shared borrowed-loader web result and feature-state boundary without changing web configuration discovery or public session options. It adds executed Intercom broker/group/lease evidence without changing the broker lifecycle. No usage/configuration changes require a user guide edit. `ISSUES.md`'s G remaining-risk line is deliberately unchanged: isolation is not complete.

### OAuth and remaining G continuation

The approved boundary now keeps SDK OAuth authorization state and PKCE verifiers in owner-local memory while preserving durable server-name tokens/client-info storage. Each diagnostic reporter carries the existing lifecycleScope through an internal symbol; replacing a reporter during host rebind does not replace its service owner. There is no new public ownership argument or diagnostic API.

Executed continuation evidence:

| invariant | affected paths | executed assertion/log | residual gap |
| --- | --- | --- | --- |
| OAuth transient, single-flight and callback ownership | Auth storage/provider, auth lifecycle maps/barrier, callback-server leases | Three retained `mcp-oauth-owner` tests pass: same-name transients/PKCE despite sibling and standalone writes; callback waiter survives sibling overlapping release; separate owner producers make two token requests. Rebind RED lost state, GREEN `/tmp/3105-g-owner-green.log`. Source public SDK rebind and sibling-close assertion passes in `/tmp/3105-g-sdk-oauth.log`. | Same-owner startup/teardown and primary-versus-cleanup handling retain existing suites; no new built OAuth browser flow. OAuth transport close currently suppresses close errors, so complete primary/cleanup preservation is not claimed. |
| Native UI ownership | `ui-session.ts` window publication and onComplete | RED closed first window when sibling started; GREEN `/tmp/3105-g-ui-green.log` proves sibling survival, repeated close, and immediate closure of a window resolving after its handle closed. | OS window opening is the mocked external boundary. Failed native window cleanup and startup transfer/reentrancy still need owner-specific evidence. |
| Provider configuration caches | Gemini API/search/browser, Exa, Perplexity, GitHub, video and YouTube config | `/tmp/3105-g-config-all-{red,green}.log`, `/tmp/3105-g-model-{red,green}.log`, `/tmp/3105-g-video-config-red.log`; final config test passes in `/tmp/3105-g-all-diagnostic-green.log`: first owner retains keys/profile/model/options, sibling observes changed file, reporter replacement preserves first cache. | Discovery paths/environment keys unchanged. This is not isolation of durable config, rate-limit budgets or all browser-cookie/native availability caches. |
| GitHub clone ownership | Clone map, config reset and local clone paths | RED shared one clone; GREEN `/tmp/3105-g-clone-green.log`: same repo produces distinct owner paths, sibling repeated clear deletes only sibling files, first cache remains usable without recloning. | External clone command is mocked. Suspended clone completion after its own cleanup and transfer/reentrant cleanup are not yet established. |
| Unavailable provider/extractor | Built public `web_search` and `fetch_content` | `sdk-host-web-unavailable.mjs`: unavailable Gemini yields explicit error, missing ffmpeg/ffprobe yields `all_failed` with zero successful results; stdout `{"verified":true}`, stderr empty, natural Node exit (`/tmp/3105-g-web-unavailable{,-stderr}.log`). Existing production error behavior already met this assertion. | Does not exercise every optional extractor dependency or third-party module-load failure. |
| Owner diagnostics and redaction | MCP config/auth/cleanup; web config/provider/curator/cleanup; Intercom config/relay/disconnect | `/tmp/3105-g-all-diagnostic-{red,green}.log`: corrupt credential/config files yield three source-attributed owner reports, no console error, no secret text. Existing built MCP sink/no-sink tests and complete host parity pass. Standalone fallback diagnostics remain compatible. | Runtime callsites route through owner context, but no all-callsite execution claim. Construction-time errors before runner binding, shared callback-server events outside an admitted owner, and third-party output remain unaudited. Browser-page scripts and separate broker process output are intentionally not host diagnostics. |
| Final continuation gates | Complete source/host parity, affected service suites plus changelog immutability, build and check | `npm run build` and `npm run check` exit 0 (`/tmp/3105-g-{build,check}-verified.log`). Source SDK parity 163/163 (`/tmp/3105-g-sdk-verified.log`); host parity 220/220 (`/tmp/3105-g-host-verified.log`); affected/changelog suites 104 files, 1036/1036 (`/tmp/3105-g-services-verified.log`), all exit 0. | Gates validate this partial candidate, not complete G isolation. First affected run caught missing copied diagnostics fixture dependency and standalone error-detail regression; both repaired without weakening assertions, targeted 9/9 and full rerun pass. |

G remains incomplete. The earlier remaining-risk entry in `ISSUES.md` must not be removed. Complete remaining-map assertions are still required for the residual lifecycle, construction-time diagnostic and external-dependency paths; packed-consumer/publication work remains outside this slice.
