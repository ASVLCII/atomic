# SDK parity #3105: slice G invariant map

Baseline: `0fcbb98f6ac5fb5cc990c28d75ace9e3b8ad109c`, branch `feat/3105-sdk-g`. This map supplements F's lifecycle map; F ownership, authority retirement and admitted-work accounting are preserved. H packed consumers and publication are outside this slice.

## Source-grounded boundaries before edits

| Invariant | Materially distinct paths and ownership transitions | Dependencies / unaffected paths |
| --- | --- | --- |
| Node subprocesses settle only after owned process/output cleanup | `web-access/subprocess.ts:runBunSubprocess` is shared by video and YouTube duration/frame/stream extraction. It unconditionally calls Bun.spawn/sleep. Caller cancellation, timeout and output overflow terminate the child; error paths currently return before joining exit. | Preserve binary bytes, separate stderr cap, cwd/env and explicit ENOENT/nonzero errors. `github-extract.ts` uses node execFile already and does not call this adapter. No CLI subprocess is needed. |
| Service initialization remains lazy | MCP `index.ts:session_start` registered cached direct tools then unconditionally initialized and scheduled warmup; cleanup imports OAuth even for never-used instances. Intercom/web light wrappers defer heavy import using generation leases. | Keep direct tools discoverable from config/cache; configured eager servers may connect at SDK creation. Preserve F candidate cleanup and stale lease fencing. |
| MCP closes only its own connections/auth/UI | Manager and lifecycle are instance-local in `init.ts`; OAuth maps/barrier in `mcp-auth-flow.ts`, callback server/port, and active Glimpse window are module-global. Agent paths consult environment, not factory agentDir. | Wrapper, direct-tool, command and reconnect paths share manager/auth helpers. Cache is disk-backed keyed by server config hash, but credential/config location must follow owner. Retain auth callback CSRF checks and primary/cleanup errors. |
| Web results/config/background work belong to one session | `storage.ts` has a global result map; `web-search-features.ts` has global pendingFetches/runtimeState/activityState. Provider modules cache credential config at module scope. `github-extract.ts` globally caches clones and removes clone paths on session changes. | Light wrapper leases do not isolate these globals. Content tools, search, curator, session restore and shutdown share them. Immutable parsers and SQLite module imports can remain shared. |
| Intercom identity and authority are owner-specific | Heavy factory owns client, reply trackers and authorization registry. It writes/restores ambient INTERCOM_SESSION_ID; runtime-group uses session-keyed env entries. Broker is a separate process with Node launch support, socket registration is the client's lease. | Same-group and supervisor authorization remain broker checks, not tool-presence checks. Disconnect must unregister one identity only; never shut down the shared broker to close a session. Broker signal/exit handlers run in its separate executable, not the SDK host. |
| Diagnostics route to the owner without secrets or unsolicited output | HostDiagnostic exists on runner bindings, but service helpers still use console. MCP config, auth, logger and cleanup; Intercom light/heavy cleanup; web config/provider/extractor paths all share this requirement. | Operational diagnostics must not include remote error bodies, credentials, prompts or supervisor capabilities. Third-party console output is not intercepted. Existing error/tool results remain available. |
| Process state is not session authority | SDK sets one default stream adapter at module load; explicit Agent streamFn uses per-session runtime. F scopes workflow registration via lifecycleScope. Theme rendering is optional; service modules must not mutate cwd, stdin, signal handlers or console. | Preserve F workflow leases, input identity/cancellation, borrowed runtime semantics and resource loader discovery. Audit theme and environment call sites before claiming unaffected behavior. |

## Planned vertical evidence

Start with a Node-only shared subprocess regression, then service public SDK probes one boundary at a time. Reuse existing MCP URL interpolation/cleanup, Intercom group/auth/lease and web subprocess suites. Built non-TTY fixtures must exit naturally; no process.exit mask. Test independent release while operations are suspended, distinguish fresh admission from completion and retain primary plus cleanup failures.

## Executed coverage

| invariant | affected paths | exact executed assertion/log and candidate | residual gap |
| --- | --- | --- | --- |
| Node execution without Bun | Shared `runBunSubprocess`, video and YouTube callers | Inherited RED: `Node subprocess adapter executes without Bun and preserves binary output` failed `Bun is not defined`, `/tmp/3105-g-subprocess-red.log`. This continuation reran all 7 subprocess tests successfully in `/tmp/3105-g-services.log`. | Explicit separate stderr cap and independently held cancellation/overlap still need expanded assertions; no built web tool fixture. |
| Headless MCP lazy discovery | Default public SDK factory, uncached configured direct tools, first owned gateway use, local HTTP transport | RED `/tmp/3105-g-lazy-red.log`: startup made 7 HTTP requests where 0 were required, with no unhandled fixture errors. GREEN `/tmp/3105-g-lazy-green.log`, then complete source parity `/tmp/3105-g-sdk.log`: 160/160, exit 0. | Eager/keep-alive and terminal startup retain initialization. Mixed eager/lazy warmup, configured cached direct execution and stdio transport still need G-specific probes. |
| HTTP connection lifetime | Built public Node SDK, two distinct project directories, close second session then call first session's live HTTP MCP connection | `built Node lazy MCP HTTP ownership`, complete host parity `/tmp/3105-g-host.log`: 215/215, exit 0. Standalone `/tmp/3105-g-http-stdout.log`: `{"verified":true,"requests":8}`; stderr empty; natural exit 0. | This proves one connection control only, not OAuth/UI, diagnostic routing, group isolation, suspended cancellation or overlapping cleanup. |
| Preserve existing service/lifecycle behavior | All matching MCP, Intercom and web-access unit suites; explicit eager acquisition in existing retired-initializer fixtures | `npx vitest --run --project unit test/unit/mcp-*.test.ts test/unit/intercom-*.test.ts test/unit/web-access-*.test.ts`: 95 files, 1004/1004, exit 0; `/tmp/3105-g-services.log`. | Existing unit coverage is not new end-to-end service isolation evidence. |
| Build/type safety | Current partial implementation | `npm run build`, `/tmp/3105-g-build.log`, exit 0; `npm run check`, `/tmp/3105-g-check.log`, exit 0 (includes both typecheck passes and shrinkwrap check). | Slice G remains incomplete despite passing gates. |
| Service ownership/config/diagnostics | Remaining paths above | Source audit only for OAuth globals, Intercom identity, web state and owner diagnostic routing. | Remaining G isolation and redaction acceptance unverified; no complete MCP/Intercom/web isolation claim. |

## Frozen web configuration boundary

Keep existing `getUserConfigPaths("web-search.json")` discovery and existing environment keys. Do not add an agentDir/project-local web configuration schema or session option, snapshot global credentials, or mutate the environment to emulate isolation. G isolates owner-scoped in-memory results, provider configuration caches, pending fetches, clone maps, and diagnostics. Existing model credentials remain separate from web API keys. This decision supersedes the earlier open web configuration question.

## Continuation audit

Source inspection in the continuation confirms that `ExtensionAPI.lifecycleScope` supplies an internal owner identity, while the diagnostic sink is private to `ExtensionRunner` and used by `emitError`. Neither the extension API nor extension context currently exposes service diagnostic emission. MCP OAuth pending authentication/transport maps, cleanup barrier, callback server, callback port, and callback waiters remain module-global. Web result storage and feature pending-fetch/runtime/activity state also remain module-global. These findings are not execution evidence and do not establish isolation.

No F lifecycle source changed and no H work or publication was performed. Root `ISSUES.md` is unchanged because isolation is not yet complete. The frozen diagnostic decision remains the implementation contract: reuse `HostDiagnostic` and the existing owner listener internally, without a new public API or global console interception. No diagnostic implementation was added in this continuation.
